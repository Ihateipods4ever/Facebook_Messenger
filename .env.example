# Copy this file to .env and fill in the real values for local development
# Do NOT commit your real .env file. .env is already in .gitignore.

# Postgres connection string format:
# postgres://USERNAME:PASSWORD@HOST:PORT/DATABASE_NAME
DATABASE_URL=postgres://messenger:1234@localhost:5432/messenger_dev

# Application port (optional, defaults to 5000)
PORT=5000
