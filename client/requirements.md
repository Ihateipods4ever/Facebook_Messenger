## Packages
framer-motion | Complex animations for the hero and features sections
lucide-react | Icons for features and UI elements (already in base, but ensuring availability)
zod | Schema validation (already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["Inter", "sans-serif"],
  display: ["Cal Sans", "Inter", "sans-serif"],
  mono: ["JetBrains Mono", "monospace"],
}
